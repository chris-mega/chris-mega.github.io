/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.licdn.com', 'images.unsplash.com', 'opengraph.githubassets.com']
    }
};

export default nextConfig;
